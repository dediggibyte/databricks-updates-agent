# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env
*.local

# Generated output — the static site is built in CI and deployed as a Pages
# artifact only; the email body is built per-run by the notify workflow.
site/
email/

# Local agent tooling (session-generated permission allowlists)
.claude/

# OS
.DS_Store

# Playwright downloads (browsers are provisioned by CI / environment)
.cache/
