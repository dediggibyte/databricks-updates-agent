This page explains how default storage on Databricks works and how to create catalogs and data objects that use it.

## What is default storage?

Default storage is a fully managed object storage platform that provides ready-to-use storage in your Databricks account. Some Databricks features use default storage as an alternative to external storage.

Serverless workspaces use default storage for internal and workspace storage, and for the default catalog that gets created with the workspace. In serverless workspaces, you can create additional catalogs in either default storage or in your own [cloud object storage](/aws/en/connect/unity-catalog/cloud-storage/).

In both classic workspaces and serverless workspaces, default storage is used by features to store things like control plane metadata, derived data, models, and other artifacts. For example, [Clean Rooms](/aws/en/clean-rooms/), [Data Classification](/aws/en/data-governance/unity-catalog/data-classification), [Anomaly detection](/aws/en/data-governance/unity-catalog/data-quality-monitoring/anomaly-detection/), [Knowledge Assistant](/aws/en/agents/agent-bricks/knowledge-assistant), and [automated session restoration for serverless notebooks](/aws/en/notebooks/notebook-compute#restore) all use a workspace's default storage. Refer to the individual feature documentation for details on what each feature stores on default storage.

note

For information on monitoring default storage costs in your account, see [Monitor default storage costs](/aws/en/admin/usage/default-storage).

## Requirements

* Creating catalogs on default storage is only available in [serverless workspaces](/aws/en/admin/workspace/serverless-workspaces).
* By default, catalogs that use default storage are only accessible from the workspace where they are created. You can grant other workspaces access, including classic workspaces, but they must use serverless compute to access data in the catalog. See [Workspace-catalog binding](/aws/en/data-governance/unity-catalog/access-control/workspace-catalog-binding).
* You must have `CREATE CATALOG` privileges to create a catalog with default storage. See [Unity Catalog privileges reference](/aws/en/data-governance/unity-catalog/access-control/privileges-reference).
* If your client is using the Databricks ODBC driver to access a default storage catalog from behind a firewall, you must configure your firewall to allow access to Databricks regional storage gateways. For IP and domain name details for default storage, see [IP addresses and domains for Databricks services and assets](/aws/en/resources/ip-domain-region).

## Create a new catalog

Complete the following steps to create a new catalog using default storage:

1. Click  **Catalog** in the sidebar. Catalog Explorer appears.
2. Click **Create catalog**. The **Create a new catalog** dialog appears.
3. Provide a **Catalog name** that is unique in your account.
4. Select the option to **Use default storage**.
5. Click **Create**.

In serverless workspaces, you can also use the following SQL command to create a new catalog in your default storage. You do not need to specify a location for the catalog.

SQL

```
CREATE CATALOG [ IF NOT EXISTS ] catalog_name  
    [ COMMENT comment ]
```

## Work with default storage

All interactions with default storage require serverless, Unity Catalog-enabled compute.

Resources backed by default storage use the same privilege model as other objects in Unity Catalog. You must have sufficient privileges to create, view, query, or modify data objects. See [Unity Catalog privileges reference](/aws/en/data-governance/unity-catalog/access-control/privileges-reference).

You work with default storage by creating and interacting with managed tables and managed volumes backed by default storage. See [Unity Catalog managed tables for Delta Lake and Apache Iceberg](/aws/en/tables/managed) and [What are Unity Catalog volumes?](/aws/en/volumes/).

You can use Catalog Explorer, notebooks, the SQL editor, and dashboards to interact with data objects stored in default storage.

### Example tasks

The following are examples of tasks you can complete with default storage:

* Upload local files to a managed volume or to create a managed table. See [Work with files in Unity Catalog volumes](/aws/en/volumes/volume-files) and [Create or modify a table using file upload](/aws/en/ingestion/create-or-modify-table).
* Query data with notebooks. See [Tutorial: Query and visualize data from a notebook](/aws/en/getting-started/quick-start).
* Create a dashboard. See [Create a dashboard](/aws/en/dashboards/tutorials/create-dashboard).
* Query data with SQL and schedule SQL queries. See [Write queries and explore data in the new SQL editor](/aws/en/sql/user/sql-editor/).
* Ingest data from an external volume to a managed table. See [Using Auto Loader with Unity Catalog](/aws/en/ingestion/cloud-object-storage/auto-loader/unity-catalog).
* Ingest data to a managed table with Fivetran. See [Connect to Fivetran](/aws/en/partners/ingestion/fivetran).
* Use BI tools to explore managed tables. See [Connect Tableau and Databricks](/aws/en/partners/bi/tableau) and [Power BI with Databricks](/aws/en/partners/bi/power-bi).
* Run serverless notebooks. See [Serverless compute for notebooks](/aws/en/compute/serverless/notebooks).
* Run serverless jobs. See [Run your Lakeflow Jobs with serverless compute for workflows](/aws/en/jobs/run-serverless-jobs).
* Run model serving endpoints. See [Deploy models using Model Serving](/aws/en/machine-learning/model-serving/).
* Run serverless Lakeflow Spark Declarative Pipelines. See [Configure a serverless pipeline](/aws/en/ldp/serverless).
* Use predictive optimization on your tables. See [Predictive optimization for Unity Catalog managed tables](/aws/en/optimizations/predictive-optimization).

## Storage lifecycle after dropping an object

When you delete a managed table or volume that uses default storage, the data files are retained to allow for a 7-day recovery window. After the recovery window ends, the data files are permanently deleted (purged) within 48 hours. Storage billing continues during the recovery window and stops once the recovery window ends. For details about the data file lifecycle, billing, and recovery options, see [Object storage lifecycle in Unity Catalog](/aws/en/data-governance/unity-catalog/object-storage-lifecycle).

## Limitations

The following limitations apply:

* Classic compute (any compute that is not serverless) cannot interact with data assets in default storage.
* OpenSharing supports sharing tables to any recipient—open or Databricks—and recipients can use classic compute to access shared tables ([Beta](/aws/en/release-notes/release-types)). Enable the **OpenSharing for Default Storage – Expanded Access** feature in your account console.
  + All other shareable assets can only be openshared with Databricks recipients on the same cloud. Recipients must use serverless compute.
* Tables with partitioning enabled cannot be openshared.
* External Iceberg and Delta clients cannot directly access the underlying metadata, manifest list, and data files for UC tables on default storage (FileIO access is not supported). However, BI tools such as Power BI and Tableau can access Unity Catalog tables on default storage using ODBC and JDBC drivers. External clients can also access Unity Catalog volumes on default storage using the Files API.
* Default storage supports external access via Databricks [ODBC](/aws/en/integrations/odbc/) and [JDBC](/aws/en/integrations/jdbc/) drivers, including the ODBC driver's [Cloud Fetch](/aws/en/integrations/odbc/capability#cloud-fetch-in-odbc) performance optimization for queries over larger datasets. However, if you are accessing a default storage table from a workspace that has front-end Private Link enabled, your ODBC client queries that return results larger than approximately 5 MiB will fail because the Cloud Fetch optimization for default storage tables does not currently support front-end Private Link.
* Credential vending is not supported, such as when external systems connect to the Unity REST API or Iceberg REST catalog. See [Unity Catalog credential vending for external system access](/aws/en/external-access/credential-vending).