Your Databricks apps can integrate with various Databricks platform features, such as Databricks SQL for querying data, Lakeflow Jobs for data ingestion and processing, Model Serving to access generative AI models, and Databricks secrets for managing sensitive information. In the context of apps, these platform features are referred to as *resources*.

## Why use resources?

To keep apps portable and secure, avoid hardcoding resource IDs. For example, instead of embedding a fixed SQL warehouse ID in your app code, configure the SQL warehouse as a resource through the Databricks Apps UI.

Using Databricks Apps resources instead of direct API calls has several benefits:

* Automatically manages credentials and permissions, which eliminates manual token handling in your code.
* Handles resource paths, endpoints, and connection details automatically.
* Provides secure, managed access instead of requiring self-managed credentials for direct API calls.
* Keeps apps portable between environments without code changes.

## Supported resource types

The following table lists all resource types that you can add to your Databricks app:

| Resource type | Description | Default resource key | Available permissions |
| --- | --- | --- | --- |
| [Databricks app](/aws/en/dev-tools/databricks-apps/apps-resource) | App-to-app communication and invocation | `app` | * Can use |
| [Genie Agent](/aws/en/dev-tools/databricks-apps/genie) | AI-powered analytics interface for natural language queries | `genie-space` | * Can view * Can run * Can edit * Can manage |
| [Lakebase database](/aws/en/dev-tools/databricks-apps/lakebase) | PostgreSQL database for data storage and querying | `postgres` (Lakebase Autoscaling), `database` (Lakebase Provisioned) | * Can connect and create |
| [Lakeflow job](/aws/en/dev-tools/databricks-apps/lakeflow) | Data ingestion and processing workflows | `job` | * Can view * Can manage run * Can manage |
| [MLflow experiments](/aws/en/dev-tools/databricks-apps/mlflow) | AI application observability, evaluation, and experiment tracking | `experiment` | * Can read * Can edit * Can manage |
| [Model serving endpoint](/aws/en/dev-tools/databricks-apps/model-serving) | Machine learning model deployment for inference requests | `serving-endpoint` | * Can view * Can query * Can manage |
| [Secret](/aws/en/dev-tools/databricks-apps/secrets) | Secure storage for sensitive values like API keys and tokens | `secret` | * Can read * Can write * Can manage |
| [SQL warehouse](/aws/en/dev-tools/databricks-apps/sql-warehouse) | Compute resources for running SQL queries | `sql-warehouse` | * Can use * Can manage |
| [Unity Catalog connection](/aws/en/dev-tools/databricks-apps/connections) | Managed connections to external data sources | `connection` | * Use Connection |
| [Unity Catalog table](/aws/en/dev-tools/databricks-apps/tables) | Structured data storage with fine-grained access control | `table` | * Select * Modify |
| [User-defined function](/aws/en/dev-tools/databricks-apps/functions) | Registered SQL and Python functions in Unity Catalog | `function` | * Can execute |
| [Unity Catalog volume](/aws/en/dev-tools/databricks-apps/uc-volumes) | File storage in Unity Catalog for app artifacts and data | `volume` | * Can read * Can read and write |
| [AI Search index](/aws/en/dev-tools/databricks-apps/vector-search) | Semantic search and similarity-based retrieval from vector embeddings | `vector-search-index` | * Can select |

## Prerequisites

To add a resource to an app, the following conditions must be met:

* The resource must exist.
* The user adding the resource must have the `Can manage` permission on the resource and the app.

Apps run with least privilege and rely on existing resources within the Databricks platform. When deployed, the app’s service principal accesses these resources and must have the necessary permissions, such as table-level access for SQL queries or read access to secrets. See [Configure authorization in a Databricks app](/aws/en/dev-tools/databricks-apps/auth).

## Configure resources for your app

Resources allow your app to securely connect to services that it depends on, without hardcoding sensitive or environment-specific values.

Add resources directly in the Databricks Apps UI when you create or edit an app.

1. In the **App resources** section when you create or edit an app, click **+ Add resource**.
2. Select the resource type you want to add.
3. Set the permissions for the app service principal on the resource.
4. Assign a key to the resource, and reference that key in your `app.yaml` file.

note

If you define your app using Declarative Automation Bundles, you specify resources in YAML as part of your bundle configuration instead of using the UI. See [app.resources](/aws/en/dev-tools/bundles/resources#app-resources).

## Manage app access to resources

Each app has a dedicated [service principal](/aws/en/dev-tools/databricks-apps/auth#app-authorization). Follow these best practices to manage access securely.

* Use the app's service principal for authentication. Never hardcode personal access tokens (PATs) in your code. Access credentials are automatically injected as environment variables:
  + `DATABRICKS_CLIENT_ID`
  + `DATABRICKS_CLIENT_SECRET`
* Don't share service principal credentials between apps or across users. Each app should use its own dedicated credentials for isolation and security.
* Apply least privilege access. Grant only the minimum required permissions to the app's service principal. For example:
  + Grant `CAN USE` on a SQL warehouse if the app only needs to run queries.
  + Grant `CAN QUERY` on a serving endpoint if the app only sends inference requests.
  + Grant `SELECT` or `MODIFY` on Unity Catalog tables based on the app’s data access needs.

## Next steps

After you add resources to your app, reference these resources in the `env` section of your app configuration using the `valueFrom` field. For instructions, see [Define environment variables in a Databricks app](/aws/en/dev-tools/databricks-apps/environment-variables). For a reference that shows the value that `valueFrom` resolves to for each resource type, see [`valueFrom` reference](/aws/en/dev-tools/databricks-apps/environment-variables#valuefrom-reference).