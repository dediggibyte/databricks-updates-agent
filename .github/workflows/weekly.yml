name: Databricks one-pagers pipeline

# Keeps the site fresh and deployed:
#   * daily 07:00 UTC — fetch new release notes, enrich, rebuild, deploy;
#   * every merge to main — rebuild from stored data/ and redeploy (no fetch,
#     no email) so template/design changes go live immediately;
#   * on-demand — backfill past release notes or re-enrich everything.
# Emails are NOT sent from here; notify.yml listens for this workflow and
# only mails the weekly digest after the Tuesday scheduled run (plus the
# month-end consolidation and manual sends).
on:
  schedule:
    - cron: "0 7 * * *"          # daily 07:00 UTC
  push:
    branches: [main]             # merge → redeploy the site
  workflow_dispatch:
    inputs:
      mode:
        description: "weekly (new notes), backfill (past notes), or reenrich (refresh all existing)"
        type: choice
        default: weekly
        options: [weekly, backfill, reenrich]
      from_month:
        description: "Backfill start month YYYY-MM (backfill mode only)"
        required: false
      to_month:
        description: "Backfill end month YYYY-MM (backfill mode only)"
        required: false
      refresh:
        description: "Re-fetch & overwrite existing notes (weekly/backfill) — updates doc links and re-enriches"
        type: boolean
        default: false
      model:
        description: "Override model id (optional, e.g. claude-opus-4-8)"
        required: false

permissions:
  contents: write        # commit refreshed data/ (site/ is never committed)
  pages: write
  id-token: write
  models: read           # free GitHub Models inference

concurrency:
  group: onepagers
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      # Check out the branch TIP, not the sha pinned at dispatch time — a run
      # queued behind another run must see the data that run committed, or the
      # deployed site goes stale relative to data/ (emails then link to 404s).
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.ref_name }}

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      # Only fetching needs a browser; merge-triggered rebuilds skip it.
      - name: Install Playwright browser
        if: github.event_name != 'push'
        run: python -m playwright install --with-deps chromium

      # Merge to main: rebuild the site from the data/ already in git and
      # deploy. No fetch, no LLM, no data commit — and notify.yml ignores
      # push-triggered runs, so no email either.
      - name: Rebuild site (merge to main)
        if: github.event_name == 'push'
        run: python -m dbx_onepager build

      - name: Run pipeline
        if: github.event_name != 'push'
        env:
          # Free GitHub Models auth. The Actions GITHUB_TOKEN only works if
          # GitHub Models is enabled for the org (Settings → Models); until
          # then it 403s and enrichment degrades to the heuristic. Optional
          # MODELS_TOKEN secret (fine-grained PAT with "Models: read") takes
          # precedence and works regardless of the org toggle.
          GITHUB_TOKEN: ${{ secrets.MODELS_TOKEN || secrets.GITHUB_TOKEN }}
          # Optional: only set if you add an ANTHROPIC_API_KEY secret and switch
          # llm.provider to "anthropic" in config.yaml.
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          MODEL_FLAG=""
          if [ -n "${{ github.event.inputs.model }}" ]; then
            MODEL_FLAG="--model ${{ github.event.inputs.model }}"
          fi
          REFRESH_FLAG=""
          if [ "${{ github.event.inputs.refresh }}" = "true" ]; then
            REFRESH_FLAG="--refresh"
          fi
          if [ "${{ github.event.inputs.mode }}" = "backfill" ]; then
            python -m dbx_onepager $MODEL_FLAG backfill \
              --from "${{ github.event.inputs.from_month }}" \
              --to "${{ github.event.inputs.to_month }}" $REFRESH_FLAG
          elif [ "${{ github.event.inputs.mode }}" = "reenrich" ]; then
            python -m dbx_onepager $MODEL_FLAG enrich --force
          else
            python -m dbx_onepager $MODEL_FLAG weekly $REFRESH_FLAG
          fi

      - name: Commit refreshed data
        if: github.event_name != 'push'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: refresh Databricks one-pagers ($(date -u +%Y-%m-%d))"
            # The branch may have moved while the pipeline ran; replay on top.
            git pull --rebase origin "$GITHUB_REF_NAME"
            git push
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy-pages:
    needs: generate
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
